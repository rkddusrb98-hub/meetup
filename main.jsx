import React from "react";
import { createRoot } from "react-dom/client";
import MeetSlot from "./MeetSlot.jsx";
createRoot(document.getElementById("root")).render(
  <React.StrictMode><MeetSlot /></React.StrictMode>
);
